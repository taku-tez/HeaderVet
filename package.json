{
  "name": "headervet",
  "version": "1.0.0",
  "description": "HTTP Security Header checker CLI — score, fix, and enforce security headers",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "headervet": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "prepare": "tsc"
  },
  "keywords": ["security", "headers", "http", "csp", "hsts", "cli", "devsecops", "sarif"],
  "author": "taku-tez",
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
